<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/D2Dark</item>
        <item name="colorPrimaryDark">@color/D2Gray</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LoginActivity" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/D2Dark</item>
        <item name="colorPrimaryDark">@color/D2Gray</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="EpisodeActivity" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/D2Dark</item>
        <item name="colorPrimaryDark">@color/D2Gray</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- 은주 -->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Divider">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">?android:attr/listDivider</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>        <!--separate line style 추가-->

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimaryDark">@color/D2Gray</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="ViewerActivity" parent="AppTheme.NoActionBar">
        <item name="colorPrimaryDark">#333333</item>
    </style>

    <style name="MainActivity" parent="AppTheme.NoActionBar">
        <item name="colorPrimaryDark">@color/D2Gray</item>
    </style>

    <style name="SearchActivity" parent="AppTheme.NoActionBar">
        <item name="colorPrimaryDark">@color/D2Gray</item>
    </style>
    <!--은주-->

    <!--석희-->
    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/colorAccent</item>
        <item name="colorControlActivated">@color/colorAccent</item>
    </style>

    <style name="ViewerGeneralActivity" parent="AppTheme.NoActionBar">
        <item name="colorPrimaryDark">@color/D2Gray</item>
    </style>
</resources>
